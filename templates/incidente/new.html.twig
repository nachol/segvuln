{% extends 'base.html.twig' %}
{% form_theme formulario 'bootstrap/bootstrap_3_layout.html.twig' %}

{% block title %}Crear/Editar Incidente{% endblock %}

    {% block body %}
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
              <a href="#">Incidentes</a>
          </li>
          <li class="breadcrumb-item active">Crear/Editar Incidente</li>
      </ol>

      {{ form_start(formulario)}}

        <div class="col-md-6">
          {{ form_widget(formulario) }}


      </div>
<a class="btn btn-default" href="{{ path('incidente') }}" role="button">Cancelar</a>
<button class="btn btn-primary" type="submit">Confirmar</button>    
</div>

{{ form_end(formulario) }}

{% endblock %}