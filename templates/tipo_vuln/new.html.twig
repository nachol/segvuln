{% extends 'base.html.twig' %}
{% form_theme formulario 'bootstrap/bootstrap_3_layout.html.twig' %}

{% block title %}Crear/Editar Tipos de Vulnerabilidades{% endblock %}

    {% block body %}
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
              <a href="#">Tipos de Vulnerabilidades</a>
          </li>
          <li class="breadcrumb-item active">Crear/Editar Tipos de Vulnerabilidades</li>
      </ol>

      {{ form_start(formulario)}}
      {{ form_errors(formulario) }}

        <div class="col-md-6">
          {{ form_widget(formulario) }}


      </div>
    
</div>
<a class="btn btn-default" href="{{ path('tipo_vuln') }}" role="button">Cancelar</a>
<button class="btn btn-primary" type="submit">Confirmar</button>
{{ form_end(formulario) }}

{% endblock %}